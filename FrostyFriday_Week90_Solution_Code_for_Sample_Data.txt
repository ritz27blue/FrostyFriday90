--モデル学習に使うTRAIＮ DATAの確認
SELECT * FROM
"FF_DB"."FF_SCHEMA"."FF90_SAMPLE_TRAIN"
LIMIT 10;
 --モデル学習
CREATE OR REPLACE SNOWFLAKE.ML.FORECAST MD (
INPUT_DATA=>TABLE("FF_DB"."FF_SCHEMA"."FF90_SAMPLE_TRAIN"),
SERIES_COLNAME=>'PRODUCT',
TIMESTAMP_COLNAME=>'DATE',
TARGET_COLNAME=>'SALES'
);
 --学習済みモデルの確認
SHOW SNOWFLAKE.ML.FORECAST;
--特徴量の重要度の確認
CALL MD!EXPLAIN_FEATURE_IMPORTANCE();
--モデルの精度の確認
CALL MD!SHOW_EVALUATION_METRICS();
 --予測対象のデータの確認
SELECT * FROM FF_DB.FF_SCHEMA.FF90_SAMPLE_PRED;
 --モデルによる予測の実施
CALL MD!FORECAST( 
INPUT_DATA=>TABLE("FF_DB"."FF_SCHEMA"."FF90_SAMPLE_PRED"),
SERIES_COLNAME => 'PRODUCT',
TIMESTAMP_COLNAME =>'DATE');
